import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.javier.rpsGame',
  appName: 'rpsGame',
  webDir: 'dist/rps-game',
  bundledWebRuntime: false
};

export default config;
